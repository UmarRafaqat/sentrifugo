pipeline {
    agent any
    stages {
        stage('Scm Checkout') {
            steps {
                git 

                }
            }
        stage('Execute Ansible') {
            steps {
                ansible
                }
            }
        }
    }
}